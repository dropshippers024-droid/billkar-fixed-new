name = "billkar-api"
main = "workers/index.ts"
compatibility_date = "2024-01-01"

[vars]
JWT_SECRET = "billkar-secret-2026-change-later"
GOOGLE_CLIENT_ID = "99456824359-62hc724e8ovd24kqkmcreot9be1p60p4.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET = "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"
GOOGLE_REDIRECT_URI = "https://api.billkar.co.in/api/auth/google/callback"
FRONTEND_URL = "https://billkar.co.in"

# Custom domain — add api.billkar.co.in in Cloudflare Dashboard first
# Workers & Pages → billkar-api → Settings → Domains & Routes → Add Custom Domain
# Then add CNAME: api.billkar.co.in → billkar-api.dropshippers024.workers.dev

[[d1_databases]]
binding = "DB"
database_name = "billkar-db"
database_id = "1eb253f1-9022-46b9-80e1-a20a3d065731"

# Uncomment after enabling R2 in Cloudflare Dashboard:
# [[r2_buckets]]
# binding = "STORAGE"
# bucket_name = "billkar-files"

wrangler pages deploy [directory]

Deploy a directory of static assets as a Pages deployment

POSITIONALS
  directory  The directory of static files to upload  [string]

GLOBAL FLAGS
      --cwd       Run as if Wrangler was started in the specified directory instead of the current working directory  [string]
      --env-file  Path to an .env file to load - can be specified multiple times - values from earlier files are overridden by values in later files  [array]
  -h, --help      Show help  [boolean]
  -v, --version   Show version number  [boolean]

OPTIONS
      --project-name        The name of the project you want to deploy to  [string]
      --branch              The name of the branch you want to deploy to  [string]
      --commit-hash         The SHA to attach to this deployment  [string]
      --commit-message      The commit message to attach to this deployment  [string]
      --commit-dirty        Whether or not the workspace should be considered dirty for this deployment  [boolean]
      --skip-caching        Skip asset caching which speeds up builds  [boolean]
      --no-bundle           Whether to run bundling on `_worker.js` before deploying  [boolean]
      --upload-source-maps  Whether to upload any server-side sourcemaps with this deployment  [boolean] [default: false]
